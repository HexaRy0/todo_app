import 'package:flutter/material.dart';

class ErrorrWidget extends StatelessWidget {
  const ErrorrWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Error'),
    );
  }
}
